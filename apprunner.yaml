version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
      - pip3 install gunicorn
      - which gunicorn
      - pip3 list | grep gunicorn
run:
  runtime-version: 3.11.9
  command: gunicorn --version
  network:
    port: 8000
