version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip3 install -r requirements.txt
      - pip3 install gunicorn==20.1.0 whitenoise==6.4.0
run:
  runtime-version: 3.11
  command: sh startup.sh
  network:
    port: 8000
  #env:
    #- name: DJANGO_SETTINGS_MODULE
     # value: erichermanndotcom.settings
